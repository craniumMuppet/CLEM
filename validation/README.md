# Validation evidence

`current` contains the evidence used by the current release and the retained
v2.29.28 numerical evidence that supports it. `history` contains completed
validation results from earlier work. The artifact contents and filenames are
preserved so historical records remain identifiable.
