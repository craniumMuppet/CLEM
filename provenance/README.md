# Provenance records

`current` contains current release metadata, package records and the active
release tree fingerprint. `history` contains the corresponding records from
earlier releases and repairs. Historical records retain their original
contents so their recorded hashes and release labels remain interpretable.
