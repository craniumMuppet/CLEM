# Internal repair-workflow history

Files in this directory preserve earlier repair-workflow labels exactly for provenance.

The labels `v2.11`, `v2.12`, and `v2.13` in these archived repair documents were internal iteration labels used during the repair process. They are **not CLEM model versions**.

These archived documents intentionally preserve the **2.29.28** repair-era identity that generated them. They are historical provenance, not the active package identity.

The authoritative current model/runtime version for this public package is **2.29.29**.
